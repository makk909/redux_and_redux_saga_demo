import { all, fork } from 'redux-saga/effects';
import {
    SignIn,
    SignUp,
    VerifyOtp,
    VerifyLoginOtp,
    MobileNumFirstStep,
    MobileNumSecondStep,
    VerifyMobileOtp,
    LogoutAction,
    ResetPin,
    ResetPinOtp,
    GetCategories,
    GetLanguages,
    EmailUpdate,
    PhoneUpdate,
    PassportUpdate,
    EmailUpdateOtp,
    PhoneUpdateOtp,
    PassportUpdateOtp,
    SkillsUpdate,
    NotifyProblemAlert,
    GetProblemAlert,
    AddProblemAlert,
    DeleteProblemAlert,
    CreateJob,
    EditJob,
    DeleteJob,
    CancelJob,
    GetJob,
    AppSettings,
    GetClientList,
    GetSolverList,
    AddPortfolioImage,
    GetPortfolioImages,
    DeletePortfolioImage,
    GetOtherUserProfile,
    jobOfferAdd,
    JobOfferEdit,
    JobOfferList,
    GetUserProfile,
    JobQuestionAdd,
    JobQuestionList,
    JobOfferAccept,
    JobOfferCancel,
    JobBookmark,
    AddReview,
    GetReview,
    AddSupport,
    CloseSupport,
    GetSupportList,
    SupportChatList,
    ChangeProfile,
    GetSolverReview,
    JobInvited,
    JobSolved,
    EditUserAddress,
    EditSpokenLanguages
    // GetSolverJobs
    // GetProducts,
} from './watchersaga';

export function* rootSaga() {

    yield all([
        fork(AppSettings),
        fork(SignIn),
        fork(SignUp),
        fork(VerifyOtp),
        fork(VerifyLoginOtp),
        fork(VerifyMobileOtp),
        fork(MobileNumFirstStep),
        fork(MobileNumSecondStep),
        fork(LogoutAction),
        fork(ResetPin),
        fork(ResetPinOtp),
        fork(GetCategories),
        fork(GetLanguages),
        fork(EmailUpdate),
        fork(PhoneUpdate),
        fork(ChangeProfile),
        fork(PassportUpdate),
        fork(EmailUpdateOtp),
        fork(PhoneUpdateOtp),
        fork(PassportUpdateOtp),
        fork(SkillsUpdate),
        fork(NotifyProblemAlert),
        fork(GetProblemAlert),
        fork(AddProblemAlert),
        fork(DeleteProblemAlert),
        fork(CreateJob),
        fork(EditJob),
        fork(DeleteJob),
        fork(GetClientList),
        fork(GetSolverList),
        fork(CancelJob),
        fork(GetJob),
        fork(AddPortfolioImage),
        fork(GetPortfolioImages),
        fork(DeletePortfolioImage),
        fork(GetUserProfile),
        fork(GetOtherUserProfile),
        fork(jobOfferAdd),
        fork(JobOfferEdit),
        fork(JobOfferList),
        fork(JobOfferAccept),
        fork(JobOfferCancel),
        fork(JobSolved),
        fork(JobInvited),
        fork(JobQuestionAdd),
        fork(JobQuestionList),
        fork(JobBookmark),
        fork(AddReview),
        fork(GetReview),
        fork(GetSolverReview),
        fork(AddSupport),
        fork(CloseSupport),
        fork(GetSupportList),
        fork(SupportChatList),
        fork(EditUserAddress),
        fork(EditSpokenLanguages)
        // fork(GetSolverJobs)
    ])
}
